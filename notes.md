# Memory Alignment
this is so that the pointer returned to the user is aligned properly
so that if long double *x = ft_malloc(sizeof(long double))
fits long double properly


# What is *page* and why does it need to be managed? 
